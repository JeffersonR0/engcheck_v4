import 'package:flutter/widgets.dart';

class LaudoListScreen extends StatelessWidget {
  const LaudoListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('teste'));
  }
}
